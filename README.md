# Men's Trip 2026 — Build 1

Upload all files and folders to the root of a public GitHub repository, then enable GitHub Pages from `main` and `/(root)`.

Recommended repository name: `mens-trip-san-francisco-2026`

The direct Airbnb URLs were not included in the source messages, so those buttons are intentionally omitted until the links are supplied.
